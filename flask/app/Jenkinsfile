#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('App') {
            steps {
                sh 'docker run -d -p 5000:5000 app:latest'
                
            }
        }
    }
}